apiVersion: v1
kind: ConfigMap
metadata:
  name: {{.Values.otherApp.name}}-config
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "1"
data:
  CONFIG_NAME: prod
  GRAYLOG_URL: test-example
  GRAYLOG_PORT: '12501'