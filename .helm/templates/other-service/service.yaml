apiVersion: v1
kind: Service
metadata:
  name: {{.Values.otherApp.name}}-service
  labels:
    app: {{.Values.otherApp.name}} 
    environment: prod 
spec:
  type: ClusterIP
  ports:
    - name: http-api
      protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: {{.Values.otherApp.name}} 
    environment: prod 