apiVersion: v1
kind: Service
metadata:
  name: {{.Values.app.name}}-service
  labels:
    app: {{.Values.app.name}} 
    environment: prod 
spec:
  type: ClusterIP
  ports:
    - name: http-api
      protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: {{.Values.app.name}} 
    environment: prod 